{
    "name": "extension",
    "version": "1.0.0",
    "description": "generating description",
    "scripts": {
        "clean": "rimraf ./dist",
        "build": "rollup -c rollup.config.mjs --silent",
        "dev": "rollup -c rollup.config.mjs -w"
    },
    "devDependencies": {
        "@rollup/plugin-typescript": "^12.1.2",
        "@types/chrome": "0.0.326",
        "rollup": "^4.43.0",
        "rollup-plugin-copy": "3.5.0",
        "rimraf": "^6.0.1",
        "typescript": "^5.7.3"
    }
}